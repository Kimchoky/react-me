import React from 'react';
import ContainerLayout from '../_common/ContainerLayout';

export default function BlogLayout({ children }: LayoutProps) {
  return (
    <ContainerLayout children={children} />
  );
}